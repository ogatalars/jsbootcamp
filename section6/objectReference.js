const palette = {
  red: "#eb4d4b",
  yellow: "#f9ca24",
  blue: "#30336b",
};

const palette2 = palette;
palette2.green = "#000  ";
console.log(palette);
console.log(palette2);
// normalmente usamos const porque não conseguimos alterar a referência
