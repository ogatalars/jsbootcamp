// selecting -> getElementById, getElementsByClassName, getElementsByTagName and querySelector
// normalmente o selecting é bem importante para o DOM, pois é com ele que iremos selecionar os elementos
// que queremos manipular.
// ver app.js
const secondID = (document.getElementById("second").style.transform =
  "rotate(90deg)");
