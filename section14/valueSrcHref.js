// some ways and method to manipulate DOM
const inputs = document.querySelectorAll("input");
inputs[0].value = "Renata";
inputs[0].value = "";
inputs[0].checked;
// a lot of things we can do with DOM and javascript to manipulate it
