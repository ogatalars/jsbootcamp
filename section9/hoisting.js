// problemas com escopos; hoisting
// normalmente o comportamente ocorre com VAR
// sempre usar let e const
// tentar declarar as variaveis ANTES de funções que vão utilizar esses valores
// function declaration are HOISTED, então usar sempre antes de tudo
// function expression não são hoistados, então usar depois de tudo
// se usarmos expressões de funções antes de declarar as variaveis, o comportamento é diferente.
